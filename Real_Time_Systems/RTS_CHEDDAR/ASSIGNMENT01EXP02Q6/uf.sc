// uf.sc: Custom scheduler assigns highest priority to the task with the least CPU utilization
Scheduling_Policy
{
    // Custom Policy: Assign highest priority to task with least utilization
    Order = least_U_first;
};
